tg2sip-webrtc (0.1.3) unstable; urgency=medium

  * Replace tgcalls backend with ntgcalls (see below), fixing the iOS-
    caller silent-audio bug.
  * Requires a new Clang+libc++ toolchain to build (ntgcalls ships a
    prebuilt WebRTC static library unconditionally compiled that way) -
    see the updated README/buildenv/build-clang-libcxx-deps.sh and
    debian/rules.
  * Fix real silent-audio bugs found during the migration: a libsrtp
    version collision between PJSIP's and WebRTC's bundled copies, a
    gating bug in ntgcalls itself (reported upstream:
    pytgcalls/ntgcalls#52), and a Microphone/Speaker device mixup on our
    own side.
  * voip_proxy_* settings are now inert (logged as a startup warning if
    set) - ntgcalls has no VoIP media proxy hook at all, unlike the old
    tgcalls backend. proxy_* (MTProto/TDLib) is unaffected.
  * Remove the now-dead third_party/tgcalls submodule and its patches.

 -- Vlad Vorobyev <vlad.vorobev@gmail.com>  Tue, 21 Jul 2026 16:21:13 +0300

tg2sip-webrtc (0.1.2) unstable; urgency=medium

  * Wire voip_proxy_* settings into tgcalls::Descriptor::proxy, so a
    configured SOCKS5 proxy actually reaches the VoIP media path (it was
    previously accepted in tg2sip.conf but silently had no effect).
  * Fix a use-after-free crash on fast call teardown: tgcalls' own
    recording thread could still be calling into TgCallsRecorder after
    TgCallsController had already been destroyed, since
    tgcalls::ThreadLocalObject's destructor doesn't wait for its async
    teardown to actually finish.
  * Stop falsely advertising video-receive capability to call peers -
    tg2sip-webrtc is audio-only and never renders video, but was reporting
    generic platform codec support regardless.

 -- Vlad Vorobyev <vlad.vorobev@gmail.com>  Fri, 17 Jul 2026 15:33:00 +0300

tg2sip-webrtc (0.1.1) unstable; urgency=medium

  * Rename the config file, everywhere it's read or shipped, from the
    original settings.ini/config.ini naming to tg2sip.conf - .ini reads as
    a Windows-ism, .conf matches normal Debian daemon convention (see e.g.
    nginx.conf, apache2.conf for the same directory/filename pattern):
    - The installed system config is now /etc/tg2sip-webrtc/tg2sip.conf
      (was /etc/tg2sip-webrtc/config.ini).
    - The tracked source template is now tg2sip.conf at the repo root
      (was settings.ini), and the build-generated, never-clobbered-in-
      place copy CMake drops next to the binary is now
      tg2sip.conf.sample (was config.ini.sample).
    - The dev/in-tree cwd fallback resolve_config_path() falls back to is
      now tg2sip.conf (was settings.ini).
    Updated every doc/packaging reference accordingly (README,
    postinst/postrm, the systemd unit, both man pages).
  * postinst now keeps [telegram] application_version in tg2sip.conf in
    sync with the installed package version (Telegram's API only uses it
    as an informational client-version string, so letting it drift is
    closer to a bug than a feature). Scoped narrowly - touches only that
    one key via dpkg-query -W + sed, and is a no-op if the value already
    matches, to avoid the "conffile modified?" prompt a broader rewrite
    would risk triggering on a future upgrade. Verified: fresh install
    sets it, reinstalling the same version is a silent no-op, and a
    stale/hand-edited value gets corrected back on the next install.
  * Rename the gen_db helper binary to tg2sip-gendb - the old name gave
    no clue which package it belonged to or what it did. Renamed the
    CMake executable target (source file tg2sip/gen_db.cpp is unchanged,
    same as tg2sip-webrtc's own main.cpp), the man page
    (debian/gen_db.1 -> debian/tg2sip-gendb.1), and every doc/packaging
    reference (README, debian/control, postinst, the systemd unit,
    buildenv/gen_db.desktop -> tg2sip-gendb.desktop).

 -- Vlad Vorobyev <vlad.vorobev@gmail.com>  Thu, 16 Jul 2026 13:13:37 +0300

tg2sip-webrtc (0.1.0) unstable; urgency=medium

  * Initial Debian packaging. Builds PJSIP 2.9 and TDLib (master) from
    source into a private prefix at package-build time (neither is
    packaged in Debian); both link statically, so the resulting
    tg2sip-webrtc/gen_db binaries carry no runtime dependency on them.
    Ships a systemd service (tg2sip-webrtc.service), a dedicated system
    user, and a config template at /etc/tg2sip-webrtc/config.ini.

 -- Vlad Vorobyev <vlad.vorobev@gmail.com>  Wed, 15 Jul 2026 16:07:21 +0300
