.TH TG2SIP-GENDB 1 "July 2026" "tg2sip-webrtc" "User Commands"
.SH NAME
tg2sip-gendb \- interactively create the TDLib login session for tg2sip-webrtc
.SH SYNOPSIS
.B tg2sip-gendb
.RI [ config-file ]
.SH DESCRIPTION
.B tg2sip-gendb
performs an interactive Telegram login (phone number, SMS code, and 2FA
password if enabled) using the
.B api_id
/
.B api_hash
configured in
.IR tg2sip.conf ,
and writes the resulting TDLib session to
.IR db.sqlite " in the current working directory (or the configured"
.B database_folder
).
.PP
Run this once, as the
.B tg2sip-webrtc
system user, before starting
.BR tg2sip-webrtc (1)
for the first time:
.PP
.RS
sudo -u tg2sip-webrtc sh -c 'cd /var/lib/tg2sip-webrtc && tg2sip-gendb'
.RE
.SH CONFIGURATION
Uses the same configuration file resolution order as
.BR tg2sip-webrtc (1).
.SH SEE ALSO
.BR tg2sip-webrtc (1)
